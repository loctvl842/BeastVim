# Daily Health Report — 2026-05-07

## Summary

🟡 — Startup is fast and stable; one sourcing event crosses the warn threshold; lint tools still not installed.

## 🟡 Startup Performance

- Startup mean: **48.84 ms** (std 8.86, 10 runs)
- vs last report (2026-05-03): +5.9 % (46.10 → 48.84 ms) — within 15 % threshold ✅
- Slowest single sourcing event: `BeastVim/init.lua` — **37.61 ms** 🟡 (warn > 30 ms, action > 60 ms)

Per-run totals (ms):
`32.81, 43.19, 39.29, 51.54, 47.59, 59.28, 51.70, 49.67, 50.51, 62.87`

## 🟢 Per-Lib Profile

Recording uptime: **58.73 ms** (much closer to actual startup vs last report's 369 ms — improvement!)

### Module require times

All modules under **1 ms** self time. No module exceeds warn threshold (5 ms). ✅

Top 3 by self time:

| Module | Self (ms) | Status |
|---|---|---|
| `beast.libs.packer.ui` | 0.71 | 🟢 |
| `beast.libs.statusline.highlights` | 0.57 | 🟢 |
| `beast.libs.key.highlights` | 0.55 | 🟢 |

Previous offenders resolved:
- `beast.libs.notify.state`: 9.06 ms → **0.05 ms** ✅
- `beast.libs.key.core`: 5.04 ms → **0.10 ms** ✅

### Function call times — top 5 by self time

| Function | Calls | Self (ms) | Threshold | Status |
|---|---|---|---|---|
| `beast.libs.packer.state.load` | 2 | **10.84** | — | 🟡 top hotspot |
| `beast.libs.packer.setup` | 1 | **2.73** | warn 10 / action 20 | 🟢 |
| `beast.libs.buf.new` | 1 | 1.91 | — | 🟢 |
| `beast.libs.statusline.components.diagnostics.provider` | 2 | 1.08 | runtime hot path | 🟢 |
| `beast.libs.key.core.safe_set` | 43 | 0.39 | — | 🟢 |

**Major improvement**: `packer.setup` dropped from 22.76 → 2.73 ms (88 % reduction). No longer over action threshold. 🎉

### Duplicated setup calls

None detected. `*.highlights` modules show CALLS = 4–5 (intentional ColorScheme reload pattern). ✅

### Notable: `packer.state.load` called twice

`packer.state.load` has CALLS = 2 with self = 10.84 ms. This is the only function with non-trivial cost being called more than once. Worth investigating whether both calls are intentional.

## 🟢 Run-time Benches

| Bench | Result | Summary |
|---|---|---|
| statusline | PASS | `BENCH name=statusline beast=4.94us lualine=89.74us ratio=18.2x threshold=1000us` |

## 🟡 Lint & Format

- luacheck: **skipped** (not installed)
- stylua: **skipped** (not installed)

Neither tool is on `$PATH`. `.luacheckrc` and `stylua.toml` exist in the repo but the tools are not installed.

## 🟢 Codemap & Dev-Spec Freshness

- Codemaps generated: **2026-05-06** (1 day ago) ✅
- Stale dev specs: none over 14 days
- Untracked dev specs (no git history): `packer-profile-ui.md`, `tabline-library.md` — likely new, not yet committed

## ⚠️ Process Gaps

- `tests/` directory is empty — standing process gap until first test lands
- `luacheck` and `stylua` not installed — lint/format checks are dead code
- Only 1 bench script exists (`bench-statusline.lua`). Missing: `bench-key.lua`, `bench-explorer.lua`, `bench-packer.lua`

## Action Items

1. [🟡] Investigate `BeastVim/init.lua` sourcing at 37.61 ms — just over the 30 ms warn. This is the init tree's total cost, so likely not actionable without splitting deferred work.
2. [🟡] Investigate why `packer.state.load` is called twice (CALLS = 2, self 10.84 ms). If both calls are needed, consider caching.
3. [🟡] Install `luacheck` and `stylua` (`brew install luacheck stylua`) or commit a decision to skip them.
4. [⚠️] Commit untracked dev specs: `packer-profile-ui.md`, `tabline-library.md`.
5. [⚠️] Add `tests/` with at least one smoke test.

## Trend

| Date | Startup mean | Std | Top fn self | Notes |
|---|---|---|---|---|
| 2026-05-07 (this report) | 48.84 ms | 8.86 ms | packer.state.load @ 10.84 ms | packer.setup fixed (2.73 ms), all module requires < 1 ms |
| 2026-05-03 | 46.10 ms | 5.07 ms | packer.setup @ 22.76 ms | First true baseline |
