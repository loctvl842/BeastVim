# Daily Health Report — 2026-05-03 (PM)

> **Second report of the day.** The morning report
> (`health-2026-05-03.md`) flagged `beast.libs.packer.setup` self-time
> (22.76 ms) over the action threshold and `beast.libs.notify.state`
> require (9.06 ms) over warn. Both are now resolved — see the Trend
> section. The morning report remains the canonical baseline; this PM
> report is the post-fix verification.

---

## 🟢 Pipelines

No CI configured. Placeholder per `health-config.md`. ✅

---

## 🟢 Data Freshness — Startup Performance

10 cold-start runs with `NVIM_APPNAME=BeastVim`:

| Stat | Value | Threshold | Status |
|---|---|---|---|
| Mean | **42.09 ms** | < 150 ms | 🟢 |
| Std | **6.66 ms** | < 20 ms | 🟢 |
| Min | 30.32 ms | — | — |
| Max | 56.15 ms | — | — |

Per-run totals (Embedded session, ms):
`56.15, 42.84, 44.59, 39.59, 36.84, 37.43, 39.97, 46.94, 46.20, 30.32`

### Top sourcing events

| Rank | self+sourced (ms) | Source |
|---|---|---|
| 1 | 18.99 | `BeastVim/init.lua` |
| 2 | 17.18 | `BeastVim/init.lua` |
| 3 | 17.17 | `BeastVim/init.lua` |
| 4 | 17.14 | `BeastVim/init.lua` |
| 5 | 16.13 | `BeastVim/init.lua` |

Slowest single source: **18.99 ms** — well under 30 ms warn. 🟢

---

## 🟢 Per-Lib Profile (`beast.profile`)

Recording uptime **41.02 ms** — matches `--startuptime` mean closely now,
so the deferred-dump warning from the morning report no longer applies on
this run.

### Function call times — top 5 by self time

| Function | Calls | Self (ms) | Threshold | Status |
|---|---|---|---|---|
| `beast.libs.packer.setup` | 1 | **1.75** | warn 10 / action 20 | 🟢 |
| `beast.libs.buf.new` | 1 | 1.59 | — | 🟢 |
| `beast.libs.packer.state.load` | 1 | 1.47 | warn 10 / action 20 | 🟢 |
| `beast.libs.statusline.components.diagnostics.provider` | 4 | 0.95 | runtime path — see `bench-*` | 🟢 |
| `beast.libs.key.core.set` | 49 | 0.38 | — | 🟢 |

`packer.setup` dropped **22.76 → 1.75 ms** (-92 %) since this morning.
This was the top action item; it is now well under both thresholds.

### Module require self times

**No module require has self time ≥ 1 ms.** Heaviest require is
`beast.libs.packer.triggers.filetype` at 0.64 ms. The morning report's
warn-list (`notify.state` 9.06 ms, `statusline.components.diagnostics`
5.29 ms, `key.core` 5.04 ms) is fully cleared. 🟢

### CALLS = 2 (intentional ColorScheme reload)

Same 5 `*.highlights` modules as before show CALLS = 2. Each second
require is sub-millisecond. 🟢 Confirmed intentional.

---

## 🟢 Run-time Render Performance

`scripts/bench-statusline.lua`:

```
BENCH name=statusline beast=14.37us lualine=90.51us ratio=6.3x threshold=1000us
```

Exit `0` (PASS). Beast statusline renders **6.3× faster than Lualine**,
two orders of magnitude under the 1 ms hard threshold. 🟢

---

## ⚠️ Process Gaps

| Gap | Status | Notes |
|---|---|---|
| Codemap freshness | 🟢 | `Generated: 2026-05-02` — 1 day old. |
| Dev specs | 🟢 | 4 files; newest 0 days, oldest 2 days. None stale. `tabline-library.md` is untracked (created today). |
| `tests/` directory | 🔴 | Still does not exist. Standing process gap. |
| `luacheck` installed | ⚠️ | Still NOT installed. `.luacheckrc` is still untracked. |
| `stylua` installed | ⚠️ | Still NOT installed. `stylua.toml` is still untracked. |
| `bench-*.lua` coverage | 🟡 | Still only `bench-statusline.lua`. No bench yet for `key`, `explorer.render`, `packer.import`, `notify`. |
| Uncommitted state | 🔴 | 4 modified + 19 untracked. Several BeastVim-essential files remain untracked: `lua/beast/profile.lua`, `scripts/`, `docs/KPI/`, `docs/tec-config/`, `docs/dev-specs/tabline-library.md`, tooling configs. |

### Notable new untracked files since morning report

`git_commit.lua`, `lua/beast/plugins/bars/`, `nvim-pack-lock.json`,
`profile.log`, `tmp/`, `neovim_docs/`, `SKILL.md`,
`.github/skills/ui-confirm/`. Some look like working-tree spillover
(`profile.log`, `tmp/`) that should be `.gitignore`d, not committed.

---

## Action Items

| # | Severity | Action | vs morning |
|---|---|---|---|
| 1 | 🔴 | Commit `lua/beast/profile.lua`. It's referenced by `init.lua`, `health-config.md`, and `debug-acquire.md`, but exists only as an untracked file. A fresh clone has no profiler. | unchanged |
| 2 | 🔴 | Decide on tooling: either commit `.luacheckrc` / `.luarc.json` / `stylua.toml` and install the tools, or delete them. Untracked-and-unused is the worst state. | unchanged |
| 3 | 🔴 | Commit (or `.gitignore`) the documentation/tooling tree: `docs/KPI/`, `docs/tec-config/`, `docs/dev-specs/tabline-library.md`, `scripts/`. These are referenced by health-config but invisible to clones. | unchanged |
| 4 | 🟡 | Add a `tests/` directory with a smoke test. Now that packer is no longer the perf hotspot, test coverage is the biggest standing gap. | unchanged |
| 5 | 🟡 | Add `bench-key.lua`, `bench-explorer.lua`, `bench-packer.lua` to match `bench-statusline.lua`'s coverage. | unchanged |
| 6 | 🟡 | Investigate the new untracked tree (`git_commit.lua`, `profile.log`, `tmp/`, `nvim-pack-lock.json`, `neovim_docs/`). Add to `.gitignore` if generated; commit if intentional. | new |
| 7 | 🟢 | Ratchet down the `packer.setup` warn threshold. With self-time now 1.75 ms, the existing `warn 10 / action 20` is too loose to catch a meaningful regression. Suggest `warn 5 / action 10`. | new — leverages the win |
| 9 | 🟢 | Update codemaps (last `Generated: 2026-05-02`) before they pass the 7-day flag — there have been at least 2 days of changes (packer perf, tabline lib, plugins/bars). | new |

---

## Trend (vs prior reports)

| Date | Startup mean | Std | Top fn self | Notes |
|---|---|---|---|---|
| 2026-05-03 PM (this report) | **42.09 ms** | 6.66 ms | packer.setup @ **1.75 ms** | Post-fix. All perf 🔴/🟡 from morning resolved. |
| 2026-05-03 AM | 46.10 ms | 5.07 ms | packer.setup @ 22.76 ms | First true baseline. |
| ~~2026-05-03 (earliest)~~ | ~~67.24 ms~~ | ~~14.94 ms~~ | ~~packer.setup @ 34.85 ms~~ | Invalid — wrong nvim config. Discarded. |

Δ AM → PM: **-8.7 % startup mean**, **-92 % top fn self**. The 1.75 ms
packer.setup is the new floor; use it as the baseline for the next check.
Std went **up** (5.07 → 6.66 ms) — still within threshold (<20 ms) but
worth noting; one slow run (56.15 ms, likely a cold disk cache hit) is
the main contributor.

---

## Next Steps

- 🔴 untracked `profile.lua` and tooling configs — `git add` + commit, or delete.
- 🟡 want me to scaffold `tests/` with a packer smoke test? → run `/tec-dev-spec`.
- 🟢 want to refresh codemaps now that packer + statusline + tabline have churned? → run `/tec-update-codemaps`.
- 🟢 propose tightening the packer threshold in `health-config.md`? → say the word and I'll edit.
