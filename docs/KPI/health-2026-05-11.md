# Daily Health Report — 2026-05-11

## Summary

🟡 — Startup improved −17.3 % vs last report (38.67 ms mean). Slowest sourcing event (init.lua 50.68 ms) exceeds 30 ms warn. stylua drift detected. luacheck still not installed.

## 🟡 Startup Performance

- Startup mean: **38.67 ms** (std 14.07, 10 runs)
- vs last report (2026-05-10): **−17.3 %** (improved from 46.77 ms)
- Slowest single sourcing event: `sourcing init.lua` — 50.68 ms ⚠️ (warn > 30 ms)
- Run 1 was a cold-cache outlier at 78.43 ms; runs 2–10 mean is **34.25 ms**
- Raw values (ms): 78.43, 28.18, 36.12, 34.21, 29.24, 31.62, 28.00, 39.79, 41.13, 39.98

## 🟢 Per-Lib Profile

- Top offender by SELF_MS (require): `beast.libs.statusline.highlights` — 0.48 ms (4 calls)
- Top offender by SELF_MS (function): `beast.libs.packer.state.load` — 2.62 ms (2 calls)
- `beast.libs.packer.setup` — 1.42 ms (1 call)
- Functions over their threshold: none (all requires < 3 ms warn; all setup functions < 10 ms warn)
- Duplicated `setup` calls: none (all setup functions called exactly once)

## 🟢 Run-time Benches

| Bench | Result | Summary |
|---|---|---|
| explorer | PASS | `BENCH name=explorer full_render=486.01us nodes=195 scenario=mixed threshold=2000us` |
| statusline | PASS | `BENCH name=statusline beast=8.62us lualine=97.68us ratio=11.3x threshold=1000us` |

## 🟡 Lint & Format

- luacheck: **not installed** — cannot verify lint status
- stylua: **FAIL** — formatting drift detected in `lua/` (e.g. `lua/beast/plugins/init.lua`)

## 🟢 Codemap & Dev-Spec Freshness

- Codemaps generated: 2026-05-06 (5 days ago)
- Stale dev specs (> 14 days): none
- Dev specs with no git history: `bench-explorer.md`, `packer-profile-ui.md`, `tabline-library.md` — may be untracked or newly created

## ⚠️ Process Gaps

- **luacheck not installed** — install via `luarocks install luacheck` to enable lint checks
- **stylua drift** — run `stylua lua/` to re-format, then commit
- **No `tests/` directory** — standing gap; no test files exist yet

## Action Items

1. [warn] Slowest sourcing event (`init.lua` — 50.68 ms) exceeds 30 ms warn threshold. Re-run the per-lib profile to identify what's driving it.
2. [warn] Run `stylua lua/` to fix formatting drift, then commit.
3. [info] Install `luacheck` (`luarocks install luacheck`) so lint drift can be detected.
4. [info] No `tests/` directory — consider adding tests when a new library is built.

### Debug Notes

Investigated "why is startup slow?" — **no regression found**. Today's mean (38.67 ms) is within 2×std of the previous report and only +3.0 % vs the 2026-05-09 baseline (37.54 ms). All thresholds green. The cold-cache first-run outlier (78 ms) is filesystem cache warming, not a code issue. See breakdown below.

## Trend

| Date | Run | Startup mean | Std | Notes |
|---|---|---|---|---|
| 2026-05-09 | daily | 37.54 ms | 4.72 | — |
| 2026-05-10 | daily | 46.77 ms | 32.94 | outlier run 1 (138 ms) |
| 2026-05-11 | daily | 38.67 ms | 14.07 | −17.3 %, improved; outlier run 1 (78 ms) |
